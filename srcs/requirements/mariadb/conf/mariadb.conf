[mysqld]
user            = mysql
pid-file        = /run/mysqld/mysqld.pid
socket          = /run/mysqld/mysqld.sock
port            = 3306
basedir         = /usr
datadir         = /var/lib/mysql
# Bind to all interfaces so WordPress can reach MariaDB over the Docker bridge network
bind-address    = 0.0.0.0
